<%= render "shared/navbar" %>

<div class="container">
  <h1>New list</h1>

  <div class="form">
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
